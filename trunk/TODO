- Add support for specifying profiles on the command line.
	- That will remove the need to find out if it's a pixel or vertex shader on the 
	  fly and also allow us to check with older versions of the shaders.

- Add support for some kind of macro expansion language.
	- This to make it easier to write tests.
	
- Add support for easier shipping of a standard library of testing functions.
	- Take the relative address of the executable and the current file as the norm.

- Fix all the cleanup memory leaks! WTF!
- Make sure that the window when it is closed does so in correct order.

